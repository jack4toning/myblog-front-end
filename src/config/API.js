const APIServer = 'http://localhost';

const blogUrl = `${APIServer}/api/blogs`;
const loginUrl = `${APIServer}/api/login`;
const logonUrl = `${APIServer}/api/logon`;

export { blogUrl, loginUrl, logonUrl };
